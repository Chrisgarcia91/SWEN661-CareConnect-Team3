import 'package:flutter/material.dart';

class RefillRequestFlow extends StatelessWidget {
  const RefillRequestFlow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Refill Request Flow')),
    );
  }
}